def simulate_patch(hemi_idx, vertno, extent):
    """
    To add patch sources, use the `sim.add_patch_sources` function according to
    its documentation:

    https://meegsim.readthedocs.io/en/latest/generated/meegsim.simulate.SourceSimulator.html#meegsim.simulate.SourceSimulator.add_patch_sources
    """
    # FILL_ME(
    #     "The `simulate_patch` function should return a `SourceConfiguration` " \
    #     "object with a patch source at (hemi_idx, vertno) that has a radius of " \
    #     "`extent` mm. Other parameters (e.g., `sfreq` or `waveform`) do not "
    #     "play a role in this example, feel free to set them arbitrarily."
    # )


def main():
    pass


if __name__ == "__main__":
    main()
